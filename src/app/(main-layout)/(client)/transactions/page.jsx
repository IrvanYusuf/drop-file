import React from 'react';
import TransactionPage from 'src/sections/client/transaction/TransactionPage';

export const metadata = { title: `Dentaloka - Transactions Page` };
const page = () => {
  return <TransactionPage />;
};

export default page;
