import React from 'react';
import ProfilePage from 'src/sections/client/profile/ProfilePage';

export const metadata = { title: `Dentaloka - Profile Page` };
const page = () => {
  return <ProfilePage />;
};

export default page;
