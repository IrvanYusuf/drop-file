import React from 'react';
import ProjectAvailablePage from 'src/sections/client/projectAvailable/ProjectAvailablePage';

export const metadata = { title: `Dentaloka - Projects Available Page` };
const page = () => {
  return <ProjectAvailablePage />;
};

export default page;
